using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/** Holds the data for the star system, right now just the id.

The id will be used to identify the tile on re-entry to the Galaxy Map, as well as fetching data from the database.
 */
public class StarTileData : MonoBehaviour
{
    public int starSystemId;

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {

    }
}
